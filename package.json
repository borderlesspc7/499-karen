{
  "name": "borderless",
  "private": true,
  "version": "1.0.0",
  "description": "Summus Edge — Cognitive Operating System (Expo SDK 54)",
  "scripts": {
    "postinstall": "npm --prefix mobile install",
    "start": "npm --prefix mobile run start",
    "android": "npm --prefix mobile run android",
    "ios": "npm --prefix mobile run ios",
    "install:all": "npm --prefix mobile install",
    "eas": "npm --prefix mobile exec eas",
    "build:apk": "npm --prefix mobile run build:android:preview"
  }
}
